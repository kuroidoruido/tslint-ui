import { SeveritiesPipe } from './severities.pipe';

describe('SeveritiesPipe', () => {
  it('create an instance', () => {
    const pipe = new SeveritiesPipe();
    expect(pipe).toBeTruthy();
  });
});
